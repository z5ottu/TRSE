name: macx

on:
  push:
    tags:
      - 'nightly'
  workflow_dispatch:
  
jobs:
  build:
    runs-on: macos-13
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      # Increment version. Ideally we would like to do it once for all builds,
      # but this is complicated as we want to allow to trigger a nightly by
      # pushing the tag, so this can't be in nightly.yml. One option would be to
      # have another intermediate workflow, but we take the quik & dirty
      # solution: increment it in each workflow and push it in one (linux.yml).
      - name: Increment version
        id: version
        run: |
          sysctl -a | grep brand
          version=$(grep "define NIGHTLY" source/LeLib/data.cpp | awk '{ print $3 }')
          let version=${version}+1
          # Mac uses BSD sed where -i takes a parameter (an extension for the
          # backup file, empty string disables backup)
          sed -i '' "s/define NIGHTLY .*/define NIGHTLY $version/" source/LeLib/data.cpp
          version=$(grep "define \(MAJOR\|FEATURE\|NIGHTLY\)" source/LeLib/data.cpp | awk '{ print $3 }' | xargs | sed 's/ /\./g')
          echo "TRSE_VERSION=${version}" >> $GITHUB_OUTPUT
      - uses: actions/setup-node@v4
      - uses: jurplel/install-qt-action@v4
        with:
          aqtversion: '==3.1.*'
          version: '6.6.0'
          host: 'mac'
          target: 'desktop'
          arch: 'clang_64'      
      - run: brew install libomp

      - name: Configure for Intel Mac build
        run: |
          # macos-13 runner is x86_64 (Intel), not ARM64
          # Change architecture from arm64 to x86_64
          sed -i '' 's|QMAKE_APPLE_DEVICE_ARCHS = arm64|QMAKE_APPLE_DEVICE_ARCHS = x86_64|g' TRSE.pro
          
          # Use Intel library paths and x86 Lua library
          sed -i '' 's|LIBS += -L\$\$PWD/libs/lua/ -lluamac_arm|LIBS += -L\$\$PWD/libs/lua/ -lluamac|g' TRSE.pro
          
          # Add Intel OpenMP paths
          sed -i '' 's|QMAKE_CXXFLAGS += -Xpreprocessor -fopenmp -I/usr/local/include|QMAKE_CXXFLAGS += -Xpreprocessor -fopenmp -I/usr/local/include -I/usr/local/opt/libomp/include|g' TRSE.pro
          sed -i '' 's|LIBS+= -L/opt/homebrew/opt/libomp/lib|LIBS+= -L/usr/local/opt/libomp/lib -L/opt/homebrew/opt/libomp/lib|g' TRSE.pro
          sed -i '' 's|QMAKE_CXXFLAGS+= -I/opt/homebrew/opt/libomp/include|QMAKE_CXXFLAGS+= -I/usr/local/opt/libomp/include -I/opt/homebrew/opt/libomp/include|g' TRSE.pro
          
          cat TRSE.pro | grep -A 20 "macx{"
          
      - run: qmake TRSE.pro
      - run: make -j 8

      - name: Package application
        run: cd Publish/publish_mac && ./publish_mac.sh nightly
      - uses: softprops/action-gh-release@v2
        with:
          name: nightly
          body: TRSE ${{ steps.version.outputs.TRSE_VERSION }} <br/> The latest automated nightly build of TRSE which succeeded. This is the recommended version to use.
          files: Publish/publish_mac/trse_osx.tar.gz
          prerelease: false
          make_latest: true
          token: ${{ secrets.TOKEN }}
